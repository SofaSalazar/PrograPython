# Implementar datos de tipo fecha

fecha y hora de import 

# Declarare una clase
# Se usara la siguiente:

clase pais :
 
# 
#

def _init_( self, nombreIng, nombreEsp, fallecidos, contagiados, pdc ):
  auto . nombreIng = nombreIng
  auto . nombreEsp = nombreEsp
  auto . fallecidos = fallecidos
  auto . contagiados = contagiados
  auto . pdc = pdc

clase Incidente = 

def _init_ (self, pais, fecha, numContagios, numFallecidos ):
  auto . pais = pais
  auto . fecha = fecha
  auto . numContagios = numContagios 
  auto . numFallecidos = numFallecidos